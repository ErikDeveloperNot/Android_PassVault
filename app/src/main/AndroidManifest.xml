<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.developernot.passvault"
    android:versionCode="1"
    android:versionName="beta-1.0.1">

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <!--
uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" /
    -->
    <application
        android:allowBackup="true"
        android:icon="@drawable/settings"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"

        android:name="android.support.multidex.MultiDexApplication">


        <activity
            android:name=".MainActivity"
            android:label="PassVault"
            android:theme="@style/Theme.AppCompat.Light.DarkActionBar">

            <!--
            intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter
            -->
        </activity>
        <activity
            android:name=".AddAccountActivity"
            android:parentActivityName=".MainActivity"
            android:theme="@android:style/Theme.DeviceDefault.Light.DarkActionBar" />
        <!--
        activity
            android:name=".AddAccountActivity"
            android:theme="@android:style/Theme.DeviceDefault.Light.DarkActionBar">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="com.developernot.passvault.MainActivity" />
        </activity
        -->
        <activity
            android:name=".EditAccountActivity"
            android:parentActivityName=".MainActivity"
            android:theme="@android:style/Theme.DeviceDefault.Light.DarkActionBar" />
        <activity
            android:name=".ExitActivity"
            android:autoRemoveFromRecents="true"
            android:theme="@android:style/Theme.NoDisplay" />
        <activity
            android:name=".LoginActivity"
            android:icon="@mipmap/ic_launcher"
            android:label="@string/app_name"
            android:roundIcon="@mipmap/ic_launcher_round"
            android:theme="@style/Theme.AppCompat.Light.DarkActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".SettingsActivity"
            android:parentActivityName=".MainActivity"
            android:theme="@android:style/Theme.DeviceDefault.Light.DarkActionBar" />
        <activity
            android:name=".PasswordGeneratorOptionsActivity"
            android:parentActivityName=".SettingsActivity"
            android:theme="@android:style/Theme.DeviceDefault.Light.DarkActionBar">
            <intent-filter>
                <action android:name="com.developernot.passvault.PasswordGeneratorOptionsActivity" />

                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>
        <activity
            android:name=".OverridePasswordGeneratorOptionsActivity"
            android:theme="@android:style/Theme.DeviceDefault.Light.DarkActionBar">

            <!--
            intent-filter>
                <action android:name="PasswordGeneratorOptionsActivity"/>
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter
            -->
        </activity>
        <activity
            android:name=".SyncActivity"
            android:label="@string/title_activity_sync"
            android:theme="@style/Theme.AppCompat.Light.DarkActionBar">

            <!-- android:theme="@style/AppTheme.NoActionBar" -->

            <intent-filter>
                <action android:name="com.developernot.passvault.SyncActivity" />

                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>
    </application>

</manifest>